// type parameter
var newArray = [];
function arrayFunction(array) {
}
// generics params
func;
';
// let variable: string = my_function(2) // errors: Type 'number' is not assignable to type 'string'.
// let variable: number = my_function('text') // errors: Type 'string' is not assignable to type 'number'.
// let variable: number = my_function(2) // it works
var variable = my_function(2); // it works also
var List = /** @class */ (function () {
    function List() {
    }
    return List;
}());
var items = [];
add(item, T);
{
    items.push(item);
}
remove(item, T);
{
    var index = items.indexOf(item);
    items.splice(index, 1);
}
var list = new List();
list.add(3);
console.log(list);
list.remove(3);
console.log(list);
